{
  "description": "googvarm frontend",
  "repository": "vegarm/googvarm",
  "license": "MIT",
  "scripts": {
    "test": "elm-test",
    "build": "rm -rf ./dist && mkdir -p ./dist && elm make ./src/Main.elm --output ./dist/app.js && cp -r ./assets/ ./dist/assets/ && cp ./index.html ./dist/"
  },
  "devDependencies": {
    "elm": "^0.19.1-3",
    "elm-format": "^0.8.3",
    "elm-live": "^4.0.2",
    "elm-test": "^0.19.1-revision2"
  },
  "dependencies": {}
}
