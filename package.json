{
	"devDependencies": {
		"elm": "^0.19.1-3",
		"elm-format": "^0.8.3",
		"elm-live": "^4.0.2",
		"normalize.css": "^8.0.1"
	},
	"scripts": {
		"serve": "live-server ./dist",
		"start": "elm-live src/Main.elm",
		"build-elm": "elm make src/Main.elm --output=dist/app.js",
		"build": "mkdir -p dist && npm run build-elm && cp -r assets dist/ && cp index.html dist/"
	},
	"dependencies": {}
}
